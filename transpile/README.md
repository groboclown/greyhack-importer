# The Transpiler

This has a model of the GreyScript language stored in an abstract syntax tree (AST). It allows converting from some
languages into

This transpiles from a subset of Python syntax into the GreyHack script language.

## Developer Installation

Developers should run:

```commandline
python -m venv .venv
. .venv/bin/activate
pip install --upgrade pip
pip install -e .
pip install tox
```

To set up a Python environment.
